import { ActiveCasesPipe } from './active-cases.pipe';

describe('ActiveCasesPipe', () => {
  it('create an instance', () => {
    const pipe = new ActiveCasesPipe();
    expect(pipe).toBeTruthy();
  });
});
